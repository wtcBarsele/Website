# Kopieer dit bestand naar `.env.local` en vul in met je echte waarden.
# .env.local wordt door .gitignore overgeslagen, dus dit is veilig om lokaal te bewaren.

# Vul deze in vanaf fase 2. In fase 1 (publieke pagina's) heb je ze nog niet nodig.
NEXT_PUBLIC_SUPABASE_URL=https://JOUW-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=jouw-anon-key
